<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <RootNamespace>Wickra.Copilot</RootNamespace>
    <AssemblyName>Wickra.Copilot</AssemblyName>

    <PackageId>Wickra.Copilot</PackageId>
    <Version>0.1.0</Version>
    <Authors>kingchenc</Authors>
    <Description>The deterministic market-context core for .NET over the Wickra C ABI: build a Copilot from a spec, drive it with commands, read back a ranked list of hard facts. The LLM adapter is never exposed over the binding.</Description>
    <PackageLicenseExpression>MIT OR Apache-2.0</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/wickra-lib/wickra-copilot</PackageProjectUrl>
    <RepositoryUrl>https://github.com/wickra-lib/wickra-copilot</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>trading;llm;copilot;microstructure;finance;ffi;native</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <NoWarn>$(NoWarn);NU5128;CS1591</NoWarn>
  </PropertyGroup>

  <!--
    Supported native runtime identifiers. The release pipeline builds the C ABI
    per target triple and stages the libraries under
    WickraCopilot/runtimes/<rid>/native/ before `dotnet pack`:
      win-x64   win-arm64   linux-x64   linux-arm64   osx-x64   osx-arm64
  -->
  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
    <None Include="runtimes/**/native/*" Pack="true" PackagePath="runtimes/" Condition="Exists('runtimes')" />
  </ItemGroup>

</Project>
