# On Windows the C ABI DLL is linked directly (mingw resolves the import stubs
# from wickra_copilot.dll); the loader finds it via PATH at run time.
PKG_CPPFLAGS = -I$(WKCOPILOT_INC)
PKG_LIBS = -L$(WKCOPILOT_LIB) -l:wickra_copilot.dll
